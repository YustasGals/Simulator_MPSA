<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Simulator_MPSA"
        xmlns:Data="clr-namespace:Modbus.Data;assembly=NModbus4" x:Class="Simulator_MPSA.MainWindow"
        Title="Simulator_MPSA" Height="479" Width="800" Icon="Simulator_MPSA.ico" Background="#FFE1E1E1" 
        >
    <Window.Resources>
        <!--
            XML Source
            
            The actual file is saved in the root directory of the
            project and its Build Action is set to "Resource" in
            the file's properties window. This causes the file to
            be included in the assembly, and makes it available
            to the application as a resource.
        -->
        <XmlDataProvider x:Key="xmldata"
                         Source="Simconfig.xml"
                         XPath="/root" />
        <!--
            Data Templates for each XML tag type expected in file.
            
            DataType        - Matches the tag name
            ItemsSource     - For tags that have descendents this
                              Henables the tree view to find them.
            XPath           - Specifies the attribute name to use.
            
            When the Xaml parser sees a tag with one of the names
            specified in DataType, it will use the associated
            template to display data about that node.
        -->
        <HierarchicalDataTemplate DataType="Node"
                                  ItemsSource="{Binding XPath=./*}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0"
                           Text="Node:" />
                <TextBlock Margin="5,0,0,0"
                           Text="{Binding XPath=@name}" />
            </StackPanel>
        </HierarchicalDataTemplate>
        <HierarchicalDataTemplate DataType="leaf">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0"
                           Text="Leaf:" />
                <TextBlock Margin="5,0,0,0"
                           Text="{Binding XPath=@name}" />
            </StackPanel>
        </HierarchicalDataTemplate>
        <HierarchicalDataTemplate DataType="root"
                                  ItemsSource="{Binding XPath=./*}">
            <TextBlock Margin="0"
                       Text="ROOT" />
        </HierarchicalDataTemplate>


    </Window.Resources>

    <Grid Loaded="Grid_Loaded"
          DataContext="{Binding TagSource, RelativeSource={RelativeSource FindAncestor, AncestorLevel=1, AncestorType={x:Type local:MainWindow}}}" Height="442" VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="0,20,0,402">
            <Button x:Name="button" Content="Button" Height="20" VerticalAlignment="Top" Width="34"/>
        </DockPanel>

        <ProgressBar x:Name="progressBar" Minimum="0" Maximum="65535" Value="{Binding Reg0}"
                     VerticalAlignment="Center" Height="20" Margin="580,88,14,334"/>
        <TextBlock Text="{Binding Reg0, FallbackValue=Test}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="506,88,236,334" Width="50" Height="20"/>
        <TextBox x:Name="textBoxIP" HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="192.168.201.1" VerticalAlignment="Top" Width="120" Margin="376,53,0,0"/>
        <TextBox x:Name="textBoxStartAdress" HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="23170" VerticalAlignment="Top" Width="54" Margin="502,53,0,0"/>
        <TextBlock Text="{Binding Reg1, FallbackValue=Test}" x:Name="textBlockOut" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Height="22" Width="113" Margin="377,88,0,0"/>
        <TextBox x:Name="textBoxNReg" HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="125" VerticalAlignment="Top" Width="72" Margin="580,53,0,0"/>
        <TextBox x:Name="textBoxAreaR" HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="2000" VerticalAlignment="Top" Width="72" Margin="662,53,0,0"/>
        <Label x:Name="label" Content="IP" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="377,29,0,0" Height="23" Width="20"/>
        <Label x:Name="label1" Content="StartAdr" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="503,29,0,0" Height="23" Width="50"/>
        <Label x:Name="label2" Content="NReg" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="581,29,0,0" Height="23" Width="36"/>
        <Label x:Name="label3" Content="AreaR" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="662,29,0,0" Height="23" Width="39"/>
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="23" Margin="376,115,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="416"/>
        <TextBox x:Name="textBoxTout" HorizontalAlignment="Left" Height="23" TextWrapping="Wrap" Text="200" VerticalAlignment="Top" Width="36" Margin="749,53,0,0"/>
        <Label x:Name="label3_Copy" Content="Tout" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="750,29,0,0" Height="23" Width="32"/>
        <DataGrid x:Name="dataGrid" Height="299" VerticalAlignment="Top" Margin="260,143,0,0" ItemsSource="{Binding IsAsync=True}" AlternatingRowBackground="LightBlue" AlternationCount="2" MouseUp="dataGrid_MouseUp" SelectedIndex="1" />
        <TreeView x:Name="dirTree"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                ItemsSource="{Binding Mode=OneWay, Source={StaticResource xmldata}}"
                VirtualizingPanel.IsVirtualizing="False"
                VirtualizingPanel.VirtualizationMode="Standard" Margin="0,40,0,0" Width="260" Height="392" />
        <Menu Margin="0,0,0,422">
            <MenuItem Header="_File">
                <MenuItem Header="_New" />
                <MenuItem Header="_Open" Click="MenuItem_Click" />
                <MenuItem Header="_Save" Click="MenuItem_Click_2" />
                <Separator />
                <MenuItem Header="_Exit" Click="MenuItem_Click_1" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Command="Cut" />
                <MenuItem Command="Copy" />
                <MenuItem Command="Paste" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Manage users">
                    <!-- 
                        <MenuItem.Icon>
                            <Image Source="/WpfTutorialSamples;component/Images/user.png" />
                        </MenuItem.Icon>
                        -->
                </MenuItem>
                <MenuItem Header="_Show groups" IsCheckable="True" IsChecked="True" />
            </MenuItem>

        </Menu>
    </Grid>
</Window>
